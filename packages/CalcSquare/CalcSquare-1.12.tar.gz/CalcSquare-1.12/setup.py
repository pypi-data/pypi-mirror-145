from setuptools import setup

setup(name='CalcSquare',
      version='1.12',
      description='Calculate the objects square from photo',
      packages=['CalcSquare'],
      author_email='gavrilyak.mikhail@gmail.com',
      zip_safe=False)