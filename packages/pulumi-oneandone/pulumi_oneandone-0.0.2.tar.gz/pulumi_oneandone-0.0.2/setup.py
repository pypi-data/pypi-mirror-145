# coding=utf-8
# *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import errno
from setuptools import setup, find_packages
from setuptools.command.install import install
from subprocess import check_call


VERSION = "0.0.2"
PLUGIN_VERSION = "0.0.2"

class InstallPluginCommand(install):
    def run(self):
        install.run(self)
        try:
            print("Installing the pulumi plugin")
            check_call(['pulumi', 'plugin', 'install', 'resource', 'oneandone', PLUGIN_VERSION,'--server','https://github.com/akshaychopra5207/pulumi-oneandone/releases/download/0.0.2'])
        except OSError as error:
            if error.errno == errno.ENOENT:
                print(f"""
                There was an error installing the oneandone resource provider plugin.
                It looks like `pulumi` is not installed on your system.
                Please visit https://pulumi.com/ to install the Pulumi CLI.
                You may try manually installing the plugin by running
                `pulumi plugin install resource oneandone {PLUGIN_VERSION}`
                """)
            else:
                raise


def readme():
    try:
        with open('README.md', encoding='utf-8') as f:
            return f.read()
    except FileNotFoundError:
        return "oneandone Pulumi Package - Development Version"


setup(name='pulumi_oneandone',
      version=VERSION,
      description="A Pulumi package for creating and managing oneandone cloud resources.",
      long_description=readme(),
      long_description_content_type='text/markdown',
      cmdclass={
          'install': InstallPluginCommand,
      },
      keywords='pulumi oneandone category/cloud',
      url='https://github.com/akshaychopra5207/pulumi-oneandone',
      project_urls={
          'Repository': 'https://github.com/akshaychopra5207/pulumi-oneandone'
      },
      license='Apache-2.0',
      packages=find_packages(),
      package_data={
          'pulumi_oneandone': [
              'py.typed',
              'pulumi-plugin.json',
          ]
      },
      install_requires=[
          'parver>=0.2.1',
          'pulumi>=3.0.0,<4.0.0',
          'semver>=2.8.1'
      ],
      zip_safe=False)
