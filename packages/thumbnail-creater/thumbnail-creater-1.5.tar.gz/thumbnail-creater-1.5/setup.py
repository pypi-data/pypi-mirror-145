from setuptools import setup, find_packages


setup(
    name='thumbnail-creater',
    version=1.5,
    keywords='project',
    scripts=['bin/thumbnail-creater'],
    install_requires=[
          'glob2',
      ],

)