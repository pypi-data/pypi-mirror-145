from setuptools import setup, find_packages


setup(
    name='thumbnail-creater',
    version=1.3,
    keywords='project',
    scripts=['bin/deploymenthub'],
    install_requires=[
          'glob2',
      ],

)